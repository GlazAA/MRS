# Скопируйте в .env на сервере: cp .env.example .env
# Не коммитьте .env в git!

# Пароль PostgreSQL (придумайте длинный, случайный)
POSTGRES_PASSWORD=замените-на-сложный-пароль

# Ключ подписи JWT (минимум 32 символа, случайная строка)
JWT_KEY=замените-на-случайную-строку-не-менее-32-символов
